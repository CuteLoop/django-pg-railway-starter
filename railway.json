{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python manage.py  migrate --fake users zero"
  },
  "deploy": {
    "startCommand": "python manage.py  migrate --fake users zero && gunicorn mysite.wsgi:application --bind 0.0.0.0:$PORT"
  }
}

